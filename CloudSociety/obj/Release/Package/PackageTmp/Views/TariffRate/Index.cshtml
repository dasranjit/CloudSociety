@model IEnumerable<CloudSocietyEntities.TariffRate>

@{
    ViewBag.Title = "Tariff Rate";
    ViewBag.Header = "Tariff  Rate List";
    var tariff = (CloudSocietyEntities.Tariff)ViewBag.Tariff;
}

<span class="SocName">Tariff :&nbsp;@tariff.Name</span>&nbsp;&nbsp;<span class="SocName">wef :&nbsp;@String.Format("{0:dd-MMM-yyyy}", tariff.wef)</span>
<br /><br />
<div class="buttton" style="width: 65px;">
    @Html.ActionLink("Create New", "Create", new { TariffID = tariff.TariffID })
</div>
<br />
<table border="0" cellpadding="2" cellspacing="0" class="table">
 @{
        int Count = 1;
    }
    @foreach (var item in Model)
    {
        if (Count == 1)
        {
    <tr>
        <th></th>
        <th>
            Service Type
        </th>
        <th>
            Chargeability
        </th>
        <th>
            Basis
        </th>
        <th>
            Rate
        </th>
    </tr>
        }

@*@foreach (var item in Model)*@

  <tr @if((Count % 2) == 0){ <text> class="TableRow" </text> } else { <text> class="altTableRow" </text>} >
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.TariffRateID }, new { @class = "EditDelete" }) |           
            @Html.ActionLink("Delete", "Delete", new { id = item.TariffRateID }, new { @class = "EditDelete" })
        </td>
        <td >
            @item.ServiceType.Type
        </td>
        <td>
            @(item.ServiceType.Chargeability == "O" ? "One Time" : (item.ServiceType.Chargeability == "M" ? "Monthly" : "Quarterly"))
        </td>
        <td>
            @(item.ServiceType.Basis == "M" ? "Per Member" : "Per Society")
        </td>
         <td >
            @item.Rate
        </td>
    </tr>
 Count = Count + 1;

    }
    @if (Count == 1)
    {<tr class="TableRow"><td>Currently there are no Records</td></tr>}
</table>
<p>
@Html.ActionLink("Back to Tarif List", "Index", "Tariff", null, new { @class = "EditDelete" })
</p>


