@model CloudSocietyEntities.AcCategory
@{
    ViewBag.Title = "Delete Account Category";
    ViewBag.Header = "Account Category";
    bool IsFullAccess = new CloudSociety.Controllers.AppInfoController().IsFullAccess();   
}
<div class="LabelDel">
    Are you sure you want to delete this?
</div>
<br />
@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.DrCr) 
    @Html.HiddenFor(model => model.Category) 
    @Html.ValidationSummary(false, "Following errors occurred while processing your request")    
    <fieldset style="width: 500px">
        <legend>Account Category Details</legend>
        <table>
            <tr>
                <td>
                    Category :
                </td>
                <td>
                    @Html.DisplayFor(model => model.Category)
                </td>
            </tr>
            <tr>
                <td>
                    DrCr :
                </td>
                <td>
                    @(Model.DrCr == "D" ? "Debit" : (Model.DrCr == "C" ? "Credit" : " "))
                </td>
            </tr>
            <tr>
                <td>
                    Nature :
                </td>
                <td>
                    @Html.DisplayFor(model => model.Nature)
                </td>
            </tr>
            <tr>
                <td>
                    Sequence :
                </td>
                <td>
                    @Html.DisplayFor(model => model.Sequence)
                </td>
            </tr>
            <tr>
                <td>
                    Active :
                </td>
                <td>
                    @if (Model.Active == true)
                    { <text>
                        <img alt="" src="@Url.Content("~/Content/Images/tick-green.png")"  />
                        </text> 
                    }
                    else
                    { 
                        <text>
                        <img alt="" src="@Url.Content("~/Content/Images/Copy of cancel-icon.png")" /></text> 
                    }
                </td>
            </tr>
            @if (IsFullAccess)
            {
            <tr>
                <td align="center" colspan="2">
                    <input type="submit" value="Delete" class="buttton" style="width: 45px; height: 23px;" />
                </td>
            </tr>
            }
        </table>
    </fieldset>
}
<div>
    @Html.ActionLink("Back to List", "Index", new { id = ViewBag.SocietySubscriptionID }, new { @class = "EditDelete" })
</div>
