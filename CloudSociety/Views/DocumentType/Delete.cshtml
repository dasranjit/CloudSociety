@model CloudSocietyEntities.DocumentType
@{
    ViewBag.Title = "Delete Document Type";
    ViewBag.Header = "Document Type";   
}
<div class="LabelDel">
    Are you sure you want to delete this?</div>
<br />
@using (Html.BeginForm())
{
    @Html.ValidationSummary(false, "Following errors occurred while processing your request")
    <fieldset style="width: 600px">
        <legend>Details Of Document Type</legend>
        <br />
        <table >
            <tr>
                <td>
                    Document Type :
                </td>
                <td>
                    @Model.DocumentType1
                </td>
            </tr>
            <tr>
                <td>
                    Active :
                </td>
                <td>
                    @if (Model.Active == true)
                    { <text>
                        <img src="@Url.Content("~/Content/Images/tick-green.png")"  />
                        </text> }
                    else
                    { <text>
                        <img src="@Url.Content("~/Content/Images/Copy of cancel-icon.png")" /></text> }
                </td>
            </tr>
            <tr>
                <td colspan="2">
                    <input type="submit" value="Delete" class="buttton" style="width: 45px; height: 23px;
                        vertical-align: middle;" />
                </td>
            </tr>
        </table>
    </fieldset>
}
<br />
<a href="@ViewBag.BackUrl" class="EditDelete">Back to List</a> @*@Html.ActionLink("Back to List", "Index", null, new { @class = "EditDelete" })*@
