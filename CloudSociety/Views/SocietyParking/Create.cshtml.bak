@model CloudSocietyEntities.SocietyParking
@{
    ViewBag.Title = "Create Society Parking";
    ViewBag.Header = " Society Parking";
    ViewBag.SocietyNameHeader = ViewBag.SocietyHead;
    Layout = "~/Views/Shared/SocietyLayout.cshtml";
    bool IsFullAccess = new CloudSociety.Controllers.AppInfoController().IsFullAccess();
}
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(false, "Following errors occurred while processing your request")
    <fieldset style="width: 450px">
        <legend>Create Society Parking</legend>
        <br />
        <input type="hidden" name="SocietyID" value=@ViewBag.SocietyID></input>
        <table border="0" cellpadding="0" cellspacing="0">
            <tr>
                <td>
                    Parking No :
                </td>
                <td>
                    <input type="text" name="ParkingNo" size="15" value=""></input>@*@Html.EditorFor(model => model.ParkingNo)*@
                    <span class="editor-field">@Html.ValidationMessageFor(model => model.ParkingNo)</span>
                </td>
            </tr>
            <tr>
                <td>
                    Parking Type :
                </td>
                <td>
                    @Html.DropDownList("ParkingTypeID", new SelectList(ViewBag.ParkingTypeList as System.Collections.IEnumerable, "ParkingTypeID", "Type"), "<- Select Parking Type ->")
                    <span class="editor-field">@Html.ValidationMessageFor(model => model.ParkingNo)</span>
                </td>
            </tr>
            @if (IsFullAccess)
            {
            <tr>
                <td colspan="2" align="center">
                    <input type="submit" value="Create" class="buttton" style="width: 45px; height: 23px;" />
                </td>
            </tr>
            }
        </table>
    </fieldset>
}
<div>
    @Html.ActionLink("Back to List", "Index", new { id = ViewBag.SocietySubscriptionID }, new { @class = "EditDelete" })
</div>
